build/
build_host/
build_host_5202/

# Run configs and experiment output are per-machine: they name local model
# paths, a specific target server, and device result directories. config/
# client.example.yaml is the tracked template; copy it rather than committing
# a working config.
config/
!config/client.example.yaml
experiments/

.DS_Store

# Local model weights and run output: large, per-machine, and regenerated by
# every run. models/ alone is 15 GB.
models/
result/
log/
*.log
